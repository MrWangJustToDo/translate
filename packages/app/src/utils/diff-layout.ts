/**
 * Minimum available width (columns) required for a split-pane diff view.
 *
 * Split mode lays out old/new code side by side; below this width the columns
 * get too narrow to read, so the diff falls back to unified mode.
 */
export const MIN_SPLIT_DIFF_WIDTH = 70;
